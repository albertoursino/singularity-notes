debug: false

# Check out all the models here: https://platform.openai.com/docs/models
model: gpt-4.1-nano 

number_of_articles: 10 # Number of articles to fetch from arxiv

select_best_article:
    # Number of times LLM select the best article. Related to the technique "Self-consistency".
    reasoning_paths: 5