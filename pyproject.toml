[project]
name = "nebula-notes-blog"
version = "0.1.0"
description = ""
authors = [{ name = "Alberto Ursino", email = "Alberto.Ursino@k-ai.at" }]
readme = "README.md"
requires-python = ">=3.11,<4.0"
dependencies = ["hugo (>=0.148.1,<0.149.0)", "pre-commit (>=4.2.0,<5.0.0)"]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
